name: Release

on:
  push:
    branches:
      - main

concurrency: ${{ github.workflow }}-${{ github.ref }}

permissions:
  contents: write
  pull-requests: write
  id-token: write

jobs:
  release:
    name: Release
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22
          registry-url: "https://registry.npmjs.org"

      # npm >= 11.5.1 required for OIDC trusted publishing
      - run: npm install -g npm@latest

      - name: Install dependencies
        run: npm ci

      - name: Build
        run: npm run build

      - name: Create Release Pull Request
        id: changesets
        uses: changesets/action@v1
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      # Publish separately so npm can use OIDC (no NODE_AUTH_TOKEN needed)
      # Remove _authToken and always-auth from setup-node's .npmrc so npm falls back to OIDC
      - name: Publish to npm
        if: steps.changesets.outputs.hasChangesets == 'false'
        run: |
          sed -i '/_authToken/d' "$NPM_CONFIG_USERCONFIG"
          sed -i '/always-auth/d' "$NPM_CONFIG_USERCONFIG"
          npm publish --access public --provenance
