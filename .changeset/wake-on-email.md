---
"openclaw-agentmail-listener": minor
---

Wake agent immediately on new email instead of waiting for heartbeat poll. Configurable via `wake` option: `"tools-invoke"` (default), `"hooks"`, or `"off"`.
