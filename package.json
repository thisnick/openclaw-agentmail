{
  "name": "openclaw-agentmail-listener",
  "version": "0.3.2",
  "type": "module",
  "description": "OpenClaw plugin: AgentMail listener — injects system events when emails arrive",
  "files": [
    "dist",
    "openclaw.plugin.json",
    "README.md"
  ],
  "scripts": {
    "build": "esbuild index.ts --bundle --format=esm --platform=node --outfile=dist/index.js --external:openclaw --external:@x402/fetch",
    "typecheck": "tsc --noEmit",
    "prepublishOnly": "pnpm run build"
  },
  "keywords": [
    "openclaw",
    "openclaw-plugin",
    "agentmail",
    "email"
  ],
  "repository": {
    "type": "git",
    "url": "git+https://github.com/thisnick/openclaw-agentmail.git"
  },
  "homepage": "https://github.com/thisnick/openclaw-agentmail#readme",
  "bugs": {
    "url": "https://github.com/thisnick/openclaw-agentmail/issues"
  },
  "license": "MIT",
  "engines": {
    "node": ">=22.12.0"
  },
  "packageManager": "pnpm@10.29.3",
  "openclaw": {
    "extensions": [
      "./dist/index.js"
    ]
  },
  "publishConfig": {
    "access": "public",
    "provenance": true
  },
  "dependencies": {
    "agentmail": "^0.4.2"
  },
  "devDependencies": {
    "@changesets/changelog-github": "^0.5.2",
    "@changesets/cli": "^2.29.8",
    "esbuild": "^0.25.0",
    "openclaw": "^2026.3.2",
    "typescript": "^5.4.5"
  }
}
